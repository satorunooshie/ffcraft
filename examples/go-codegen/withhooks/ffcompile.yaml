version: v1

variant_sets:
  boolean:
    on: true
    off: false

flags:
  - key: show_sample_banner
    variant_set: boolean
    default_variant: off
    environments:
      prod:
        serve: on
