version: v1
variant_sets:
  boolean:
    on: true
    off: false
flags:
  - key: fallback-default
    variant_set: boolean
    default_variant: off
    environments:
      prod:
        rules:
          - if:
              eq:
                - { var: user.country }
                - JP
            serve: on
