version: v1
variant_sets:
  boolean:
    on: true
    off: false
flags:
  - key: missing-action
    variant_set: boolean
    default_variant: off
    environments:
      prod:
        rules:
          - if:
              literal_bool: true
